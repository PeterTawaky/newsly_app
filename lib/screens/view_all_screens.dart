import 'package:flutter/material.dart';

class ViewAllScreens extends StatelessWidget {
  const ViewAllScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('View all screens')));
  }
}
